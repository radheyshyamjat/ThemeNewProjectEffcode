<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.effcode.in.themes">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.mypackage.MyGlideModule"
            tools:node="remove" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <!-- <meta-data -->
        <!-- android:name="com.mypackage.MyGlideModule" -->
        <!-- android:value="GlideModule" /> -->

        <activity
            android:name="in.effcode.App.Activity.LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="in.effcode.App.Activity.DetailActivity"
            android:screenOrientation="portrait"
            android:theme="@style/profile" />
        <activity
            android:name="in.effcode.App.Activity.Profile"
            android:screenOrientation="portrait"
            android:theme="@style/profile" />
        <activity
            android:name="in.effcode.App.Activity.LoginActivitySecond"
            android:screenOrientation="portrait"
            android:theme="@style/login" />
        <activity
            android:name="in.effcode.App.Activity.LoginActivityUI"
            android:screenOrientation="portrait"
            android:theme="@style/profile" />
        <activity android:name="in.effcode.App.Activity.OtpSendActivity"
            android:screenOrientation="portrait"
            android:theme="@style/profile" ></activity>
    </application>

</manifest>